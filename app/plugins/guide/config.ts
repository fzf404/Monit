import { PluginConfig } from '~/context/interface'

const config: PluginConfig = {
  name: 'Guide',
  width: 200,
  height: 200,
  description: {
    en: 'Quick Start',
    cn: '快速开始',
  },
}

export default config
